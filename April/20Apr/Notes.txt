JIT Compiler:

java language
 1010 -> Java 
 1010 <- Java	

JIT: Just-In-Time Compiler 

Garbage collector: It comes with JIT and helps clean the resources (Memory)
By default: this runs in backround and you do not need to anything.

You can explicitly call the garbage collector(rare)


Revision:
        // Java variables: int speed = 100; speed(variable); String name = "Alex", (name is the variable)
        // Java Data types (Primitive): int, String, char, boolean, float
        // Java Operators: +, -, *, /, ++
	// Common String functions: indexOf(" "), concat(parameter), length(), toUpperCase()
	// Common Math functions: Math.abs(param),  Math.max(para1, para2), Math.min(para1, para2), 
	   Math.sqrt(para1), Math.random() 
	// Booleans: true or false values, ex: bool flag = true;
	// if..else statement: if(condition) {} else {}

Today:

Switch statement: when you want to compare some input and jump directly to an action you use the switch statement.

Loops: we need a loop to execute same set of instructions again and again..

For Loop, For each loop, while and do while 